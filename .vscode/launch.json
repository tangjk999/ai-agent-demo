{
    // 使用本地 venv 解释器调试 main.py
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: 当前文件 (venv)",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/main.py",
            "console": "integratedTerminal",
            "python": "${workspaceFolder}/venv/bin/python"
        }
    ]
}